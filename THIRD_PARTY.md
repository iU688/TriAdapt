# Third-party attribution

GraphMLP-derived backbone files in `runtime/models/` retain the upstream MIT notice in `licenses/GRAPHMLP_MIT.txt`. Upstream: https://github.com/Vegetebird/GraphMLP.

The shared factory imports `runtime/models/wj_conv.py`, attributed to Spatio-Temporal MLP-Graph. Its MIT notice is retained in `licenses/ST_MLP_GRAPH_MIT.txt` even though this operator is not selected for the paper configuration.

Original contributions retain the MIT license in `LICENSE`. Datasets and pretrained weights are governed by their providers' independent terms, linked in [DATASETS.md](DATASETS.md).
